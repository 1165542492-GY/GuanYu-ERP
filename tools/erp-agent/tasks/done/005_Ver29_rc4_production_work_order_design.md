# 任务标题

Ver2.9-rc4 生产任务/工单设计与最小可用版本

# 阶段

Ver2.9-rc4 生产任务/工单

# 风险等级

R3 — 涉及生产任务数据模型、领料与入库关联；须先设计方案再实现，不破坏现有数据。

# 完成状态

**已完成** — 2026-06-28 00:17:41

- 报告：`docs/worklog/reports/ver29_rc4_production_work_order_20260628_001741.md`
- build：0 错误 0 警告
- 未 commit / 未 push / 未 tag

# 当前目标

**第一步：必须先输出设计方案，再做最小可用版本。**

设计方案必须包含：

- 生产任务数据模型
- 生产任务状态（如：待领料、领料中、已完成等）
- 来源：销售订单 / 机型成本 / BOM
- 应领物料清单
- 已领 / 未领 / 缺料计算规则
- 分批领用流程
- 成品 / 半成品入库关联
- 对现有生产领用和成品入库的兼容方案

**第二步：最小可用版本目标**

- 能新增生产任务
- 能查看任务应领物料
- 能分批领用
- 能看到已领/未领
- 旧生产领用仍能正常打开和使用

# 允许修改文件

- `Program.cs`（新增生产任务相关接口）
- `Business.html`（生产任务 UI）
- 可新增生产任务相关 `.cs` / `.html` 文件
- `docs/worklog/`
- `docs/worklog/reports/`

# 禁止动作

- 禁止清数据
- 禁止 commit
- 禁止 push
- 禁止同步正式 App（`D:\冠誉制造ERP\App`）
- 禁止修改稳定仓库（`C:\Users\Administrator\Documents\GuanYu-ERP-Stable`）
- 禁止删除、移动、重建 tag
- 禁止 `git reset`、`git clean`、`git push --force`
- **禁止**破坏现有 BOM、机型成本、生产领用、成品入库数据
- **禁止**做深度数据迁移
- **禁止**在未输出设计方案前直接写大量业务代码

# 执行步骤

1. 阅读现有 BOM、机型成本、生产领用、成品入库相关代码与数据结构。
2. 编写设计方案文档，写入 `docs/worklog/reports/ver29_rc4_production_design_yyyyMMdd.md`，包含上述全部设计要点。
3. 设计方案经人工确认后再进入实现（本任务内可先完成设计 + 最小可用版本）。
4. 实现生产任务数据模型与存储（兼容旧数据，不迁移破坏）。
5. 实现新增生产任务、查看应领物料 UI/API。
6. 实现分批领用，关联生产任务，展示已领/未领。
7. 验证旧生产领用入口仍可正常使用。
8. 运行 `dotnet build ERP.csproj`，确认 0 错误 0 警告。
9. 浏览器验收最小可用版本。
10. 输出设计与实现报告。

# 验收要求

- 设计方案文档完整，覆盖数据模型、状态、来源、应领/已领/缺料、分批领用、入库关联、兼容方案。
- build 成功，0 错误 0 警告。
- 可新增生产任务并查看应领物料。
- 可分批领用，已领/未领状态正确展示。
- 旧生产领用功能仍可打开，未被破坏。
- 未清数据、未做深度迁移。
- BOM、机型成本、成品入库现有功能不受影响。

# 输出报告要求

报告写入 `docs/worklog/reports/`，包含：

- 设计方案文档路径与摘要
- 执行时间与阶段（Ver2.9-rc4）
- build 结果
- 最小可用版本功能验收结果
- 新增/修改文件列表
- 与旧生产领用/成品入库兼容性验证结果
- 风险等级与后续 rc5 依赖说明
- 是否建议提交（仅记录，不自动执行）

# 完成后是否允许自动提交

否
