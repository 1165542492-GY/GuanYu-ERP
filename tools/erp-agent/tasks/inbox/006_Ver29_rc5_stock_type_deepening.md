# 任务标题

Ver2.9-rc5 仓库类型深化

# 阶段

Ver2.9-rc5 仓库类型深化

# 风险等级

R3 — 涉及库存分类与出入库流向；必须先输出迁移兼容方案，不允许直接硬改旧数据。

# 当前目标

**第一步：必须先输出迁移兼容方案，不允许直接硬改旧数据。**

目标：

- 明确原材料、半成品、成品三种仓库/库存类型。
- 库存汇总支持按类型筛选。
- 采购入库默认进原材料。
- 生产领用扣原材料/半成品。
- 半成品入库增加半成品库存。
- 成品入库增加成品库存。
- 销售出库优先扣成品。
- 兼容旧数据（无类型标记的历史库存视为原材料或按规则默认）。
- 不破坏库存闭环（入库、出库、汇总数量一致）。

# 允许修改文件

- `Program.cs`（库存类型相关逻辑）
- `Business.html`（库存展示与筛选 UI）
- 相关库存辅助文件（如 `InventoryLinkage.cs` 等，按需）
- `docs/worklog/`
- `docs/worklog/reports/`

# 禁止动作

- 禁止清数据
- 禁止 commit
- 禁止 push
- 禁止同步正式 App（`D:\冠誉制造ERP\App`）
- 禁止修改稳定仓库（`C:\Users\Administrator\Documents\GuanYu-ERP-Stable`）
- 禁止删除、移动、重建 tag
- 禁止 `git reset`、`git clean`、`git push --force`
- **禁止**直接硬改旧库存 JSON 文件
- **禁止**破坏当前库存汇总准确性
- **禁止**在未输出兼容方案前批量改写历史数据

# 执行步骤

1. 阅读现有库存、出入库、汇总相关代码与数据格式。
2. 编写迁移兼容方案文档，写入 `docs/worklog/reports/ver29_rc5_stock_type_migration_yyyyMMdd.md`，说明：
   - 旧数据如何兼容（默认类型、惰性标记、读取时推断等）
   - 各类出入库流向规则
   - 汇总按类型筛选实现方式
   - 回滚与验证策略
3. 按兼容方案实现库存类型字段（新增字段或读取层兼容，不批量改旧文件）。
4. 实现库存汇总按类型筛选 UI/API。
5. 调整采购入库、生产领用、半成品入库、成品入库、销售出库的类型流向逻辑。
6. 验证旧数据仍可正常查看与汇总。
7. 验证库存闭环：入库 + 出库 = 结余一致。
8. 运行 `dotnet build ERP.csproj`，确认 0 错误 0 警告。
9. 浏览器验收各类型出入库与汇总。
10. 输出兼容方案与实现报告。

# 验收要求

- 迁移兼容方案文档完整，未直接硬改旧库存文件。
- build 成功，0 错误 0 警告。
- 原材料、半成品、成品类型定义清晰。
- 库存汇总可按类型筛选，总数与明细一致。
- 采购入库 → 原材料；生产领用 → 扣原材料/半成品；半成品入库 → 半成品；成品入库 → 成品；销售出库 → 优先扣成品。
- 旧数据兼容，库存汇总不被破坏。
- 库存闭环验证通过。

# 输出报告要求

报告写入 `docs/worklog/reports/`，包含：

- 迁移兼容方案文档路径与摘要
- 执行时间与阶段（Ver2.9-rc5）
- build 结果
- 各类型出入库流向验收结果
- 旧数据兼容性验证结果
- 库存闭环验证结果
- 修改文件列表
- 风险等级与是否建议进入 rc6
- 是否建议提交（仅记录，不自动执行）

# 完成后是否允许自动提交

否
