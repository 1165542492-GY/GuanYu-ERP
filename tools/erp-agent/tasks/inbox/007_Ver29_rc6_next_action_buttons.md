# 任务标题

Ver2.9-rc6 业务下一步按钮

# 阶段

Ver2.9-rc6 业务下一步按钮

# 风险等级

R2 — 前端路由跳转与单据来源传递；如需轻量接口仅做参数预填，不改核心业务逻辑。

# 当前目标

- **销售订单**列表增加：生成销售出库、查看应收、登记收款。
- **采购单**列表增加：生成采购入库、查看应付、登记付款。
- **BOM/机型成本**增加：生成生产领用、生成成品入库、查看成本。
- 按钮点击后自动带来源单据，不让用户重复选择来源。

# 允许修改文件

- `Business.html`
- `App.html`（如需首页快捷入口联动）
- `Program.cs`（如确需新增轻量预填/跳转接口，不改库存/财务核心逻辑）
- `docs/worklog/`
- `docs/worklog/reports/`

# 禁止动作

- 禁止清数据
- 禁止 commit
- 禁止 push
- 禁止同步正式 App（`D:\冠誉制造ERP\App`）
- 禁止修改稳定仓库（`C:\Users\Administrator\Documents\GuanYu-ERP-Stable`）
- 禁止删除、移动、重建 tag
- 禁止 `git reset`、`git clean`、`git push --force`
- 禁止破坏原有菜单入口与独立新增流程
- 禁止修改库存扣减、应收应付生成核心逻辑

# 执行步骤

1. 运行 `dotnet build ERP.csproj`，确认 0 错误 0 警告。
2. 在**销售订单**列表添加快捷按钮：生成销售出库、查看应收、登记收款。
3. 在**采购单**列表添加快捷按钮：生成采购入库、查看应付、登记付款。
4. 在 **BOM/机型成本** 页面添加快捷按钮：生成生产领用、生成成品入库、查看成本。
5. 实现点击「生成销售出库」：跳转销售出库新增并自动带入订单信息。
6. 实现点击「生成采购入库」：跳转采购入库新增并自动带入采购单信息。
7. 实现「查看应收/应付」「登记收款/付款」：跳转对应页面并定位来源单据。
8. 实现 BOM/机型成本 → 生产领用/成品入库的带单跳转。
9. 验证原有菜单入口与独立新增流程仍可用。
10. build 并浏览器逐项验收。
11. 输出实现与验收报告。

# 验收要求

- build 成功，0 错误 0 警告。
- 从销售订单点击「生成销售出库」能跳转并带出订单（客户、产品、数量等）。
- 从采购单点击「生成采购入库」能跳转并带出采购单。
- 从应收能登记收款；从应付能登记付款。
- BOM/机型成本能生成生产领用、成品入库并带出来源。
- 原菜单入口与独立新增不受影响。
- 用户无需重复选择来源单据。

# 输出报告要求

报告写入 `docs/worklog/reports/`，文件名建议 `ver29_rc6_next_action_yyyyMMdd_HHmmss.md`，包含：

- 执行时间与阶段（Ver2.9-rc6）
- build 结果
- 各列表/页面新增按钮清单
- 逐项跳转与带单验收结果
- 原菜单入口兼容性验证
- 修改文件列表
- 风险等级与 Ver2.9 整体完成度评估
- 是否建议提交（仅记录，不自动执行）

# 完成后是否允许自动提交

否
