# Ver2.9-rc11 售后维修操作记录、权限回归与审计收口

- **执行时间**: 2026-06-28 10:52:08
- **分支**: codex/ver2.8-basic-business-framework（未 commit / push / tag）
- **基线提交**: b1a036e Ver2.9-rc10 after-sales closure enhancement

---

## 1. 修改/新增文件

| 文件 | 说明 |
|------|------|
| `AfterSalesServiceOrder.cs` | `AuditAfterSalesServiceOrder` / `FormatAfterSalesAuditDetail`；各动作结构化记录 |
| `OperationLog.cs` | `ParseAuditAction` 识别售后维修模块；SR 单号与客户名解析；状态流转/生成应收操作类型 |
| `OperationLog.html` | 筛选提示「售后维修工单」 |
| `Business.html` | 页脚版本 Ver2.9-rc11 |
| `check_ver29_pages.ps1` | 源码标记检查（操作记录/权限/生成应收/模块解析） |
| `VERSION.md` / 任务总表 / 执行台账 | rc11 条目 |
| `tools/erp-agent/tasks/done/011_Ver29_after_sales_audit_permission_closure.md` | 任务归档 |

---

## 2. 操作记录覆盖动作

| 动作 | 操作类型 | Audit 动作名 |
|------|----------|--------------|
| 新增 | 新增 | 新增售后维修工单 |
| 编辑 | 修改 | 修改售后维修工单 |
| 删除（确认后） | 删除 | 删除售后维修工单 |
| 确认派工 / 开始 / 完成 / 结算 / 取消 | 状态流转 | 售后维修状态流转 |
| 生成应收（成功后） | 生成应收 | 售后维修生成应收 |
| 测试数据总表导入 | 导入 | 复用「导入测试数据总表」（含售后 Sheet 统计） |

生成应收失败时不写成功记录（Audit 仅在 `MutateJsonList` 成功后调用）。

---

## 3. 操作记录字段摘要

每条摘要示例（Message / EntityName）：

`SR20260628001 客户:XX公司 状态:草稿→待派工 应收1000 已收0 未收1000 配件2行 确认派工`

| 字段 | 来源 |
|------|------|
| 模块 | 售后维修工单 |
| EntityType | AfterSalesServiceOrder |
| EntityCode | ServiceNo（SR 前缀） |
| EntityName | 客户名称（从摘要解析） |
| 状态变化 | 旧状态→新状态 |
| 金额 | 应收/已收/未收 |
| 关联应收 | 生成应收时 extra「应收单:YSxxx」 |

不写入配件 JSON 全文，仅「配件N行」。

---

## 4. 权限键检查结果

| 权限键 | AllPermissionKeys | PermissionGroups | 后端 API | 前端 |
|--------|-------------------|------------------|----------|------|
| after_sales.view | ✓ | ✓ 查看 | GET 列表/详情/export | 菜单 `PAGE_PERM`、asoCan view |
| after_sales.add | ✓ | ✓ 新增 | POST | asoAdd 按钮 |
| after_sales.edit | ✓ | ✓ 修改 | PUT + 状态 + 生成应收 | 编辑/下一步/流转 |
| after_sales.delete | ✓ | ✓ 删除 | DELETE | 删除按钮 |

**admin**：`HasPermission` / `IsAdminUser` → 默认全部权限。

**非 admin**：无权限时 API 403；前端 `asoCan()` 隐藏按钮；菜单 `after_sales.view` 控制入口。

---

## 5. 操作记录页面验证

| 项 | 结果 |
|----|------|
| API 新增维修单后有关键字记录 | PASS（keyword=SR20260628002） |
| 模块筛选「售后维修」 | 需重启 ERP 加载 rc11 后 Module=售后维修工单 |
| 详情弹窗 X/关闭 | 沿用 rc8 机制，未改动逻辑 |
| 内容可读 | 摘要中文，非 JSON  dump |

**说明**：当前运行中 ERP.exe 仍为旧构建时，新增记录 Module 可能显示「系统」；重启后加载 rc11 即显示「售后维修工单」。

---

## 6. build / 回归

| 项 | 结果 |
|----|------|
| `dotnet build -o bin\rc11-regression-build-check` | **0 错误 0 警告** |
| `run_ver29_regression.ps1` | **PASS**（含 4 项源码标记） |
| 未登录 API | 401 PASS |

---

## 7. 安全确认

- [x] 未 commit / push / tag
- [x] 未同步 `D:\冠誉制造ERP\App`（仅读取运行数据）
- [x] 未执行 006
- [x] 未接库存
- [x] 未破坏 rc9 弹窗 / rc10 下一步 / 测试数据 Sheet / 008 回归

---

## 8. 浏览器待人工

- 操作记录模块筛选「售后维修工单」
- 各状态流转、生成应收、删除确认后记录
- 无权限子账号菜单/按钮/API 拦截

---

## 9. rc11 追加：操作记录页收口（2026-06-28）

| 项 | 变更 |
|----|------|
| 列表列 | 默认 6 列（时间/操作人/模块/操作/摘要/详情），去掉 IP/业务编号/业务名称/结果 |
| 表格宽度 | `oplog-table-compact` min-width 860px（原 1380px） |
| SR 模块 | 读取时 `NormalizeOperationLogEntry` 归一为「售后维修工单」 |
| 摘要 | 人话示例：`新增维修单 SR20260628001，客户 XX，草稿，应收 100 元` |
| 详情 | 业务类型中文化；IP/编号等移入详情 |

复核 zip：`docs/worklog/reviews/ver29_rc11_review_*.zip`

---

*Generated by Cursor Agent — Ver2.9-rc11*
