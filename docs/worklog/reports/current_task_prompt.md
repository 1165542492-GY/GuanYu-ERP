# 当前待执行任务

> 生成时间：2026-06-27 22:47:37
> 任务文件：`C:\Users\Administrator\Documents\ERP\tools\erp-agent\tasks\running\003_Ver29_rc2_forms_linkage_verify_and_fix.md`

---

## 风险拦截提醒

任务内容未命中脚本级黑名单。

**脚本禁止自动执行：** git add/commit/push/reset/clean、清数据、同步正式 App、删除 tag 等。

---

## 任务内容

# 任务标题

Ver2.9-rc2 业务单据自动关联验收与修复

# 阶段

Ver2.9-rc2 业务单据自动关联与表单减负

# 风险等级

R2/R3 — 前端自动带出与中风险后端关联逻辑；涉及出库/入库数量校验时须谨慎，不擅自改库存扣减核心逻辑。

# 当前目标

- 验收并修复销售订单、销售出库、采购单、采购入库、生产领用、成品入库的自动带出和字段减负。
- **销售订单**：客户和物料自动带出，成本字段不干扰普通新增。
- **销售出库**：必须来源销售订单，自动带出客户、产品、数量、金额，只填本次出库数量。
- **采购单**：供应商和物料自动带出单位、价格、含税/不含税。
- **采购入库**：必须来源采购单，自动带出供应商、物料、数量、单价、金额，只填本次入库数量。
- **生产领用**：支持按 BOM 分批领用，不默认一次领完整个 BOM。
- **成品入库**：说明清楚，可关联机型成本/BOM，成本自动带出或只读。
- 单个新增和批量新增字段一致。

# 允许修改文件

- `Business.html`
- `Program.cs`（仅限单据关联、自动带出相关接口）
- `BusinessDocLinkage.cs`
- `docs/worklog/`
- `docs/worklog/reports/`

# 禁止动作

- 禁止清数据
- 禁止 commit
- 禁止 push
- 禁止同步正式 App（`D:\冠誉制造ERP\App`）
- 禁止修改稳定仓库（`C:\Users\Administrator\Documents\GuanYu-ERP-Stable`）
- 禁止删除、移动、重建 tag
- 禁止 `git reset`、`git clean`、`git push --force`
- 禁止擅自修改库存扣减、应收应付生成、财务流水联动核心逻辑
- 禁止破坏已有单据数据结构

# 执行步骤

1. 运行 `dotnet build ERP.csproj`，确认 0 错误 0 警告。
2. 启动开发版 ERP，浏览器登录。
3. **销售订单**：新增/编辑，验证选客户、选物料后字段自动带出；成本字段不影响普通新增流程。
4. **销售出库**：从销售订单生成或关联，验证客户、产品、订单数量、金额自动带出；仅填写本次出库数量。
5. **采购单**：验证选供应商、选物料后单位、价格、含税/不含税自动带出。
6. **采购入库**：从采购单关联，验证供应商、物料、数量、单价、金额自动带出；仅填本次入库数量。
7. **生产领用**：验证 BOM 分批领用，默认不全量领完；已领/未领状态清晰。
8. **成品入库**：验证机型成本/BOM 关联说明；成本自动带出或只读。
9. **批量新增**：对上述单据逐一验证批量新增与单个新增字段一致。
10. 发现问题按允许范围修复，修复后 build 并复验。
11. 输出验收与修复报告。

# 验收要求

- build 成功，0 错误 0 警告。
- 六类单据（销售订单、销售出库、采购单、采购入库、生产领用、成品入库）自动带出符合上述目标描述。
- 销售出库必须关联销售订单；采购入库必须关联采购单。
- 生产领用支持 BOM 分批，不默认全量领完。
- 成品入库成本字段只读或自动带出，有清晰说明。
- 单个新增与批量新增字段行为一致。
- 未破坏库存、应收应付、财务核心逻辑。
- 浏览器人工验收通过。

# 输出报告要求

报告写入 `docs/worklog/reports/`，文件名建议 `ver29_rc2_forms_linkage_yyyyMMdd_HHmmss.md`，包含：

- 执行时间与阶段（Ver2.9-rc2）
- build 结果
- 六类单据逐项验收结果（通过/失败/待修）
- 发现的问题与修复项清单
- 修改文件列表
- 单个 vs 批量新增一致性检查结果
- 风险等级与是否建议进入 rc3
- 是否建议提交（仅记录，不自动执行）

# 完成后是否允许自动提交

否


---

## 人工执行指引

1. 在 Cursor 中打开开发仓库：`C:\Users\Administrator\Documents\ERP`
2. 阅读上方任务内容与 `docs/worklog/ERP-Ver2.9-风险清单.md`
3. 按任务步骤执行；R2+ 任务须浏览器验收
4. 完成后可将任务文件移至 `tools/erp-agent/tasks/done/` 或再次运行本脚本

---
*由 `tools/erp-agent/run_task_loop.ps1` 自动生成*
