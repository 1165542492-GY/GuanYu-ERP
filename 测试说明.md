# 冠誉ERP — 测试说明

> 本文供开发测试、验收、上线前检查使用。  
> 建议在干净环境或测试数据环境下执行，避免影响正式业务数据。

---

## 1. 测试前准备

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 或更高 |
| 运行环境 | .NET 8 SDK 或 Desktop Runtime |
| 源码目录 | 本仓库解压后的文件夹 |
| 浏览器 | Chrome、Edge 等现代浏览器 |
| 测试账号 | admin（管理员）、至少 1 个子账号（普通用户） |

**数据目录（正式）**：

| 目录 | 用途 |
|------|------|
| `D:\冠誉制造ERP\Data` | 业务 JSON 数据 |
| `D:\冠誉制造ERP\Backups` | 自动/手动/清空前备份 |
| `D:\冠誉制造ERP\Exports` | 建议保存导出文件 |
| `D:\冠誉制造ERP\Imports` | 建议放置待导入文件 |

**建议**：

- 测试前用「测试数据导入导出」备份当前数据，或完整备份 `D:\冠誉制造ERP` 目录。
- 不要把 ERP 数据目录放在 WPS 云盘、OneDrive、百度网盘等同步目录中。
- 多电脑测试时，只在一台主机运行 ERP，其他电脑通过浏览器访问主机 IP。
- 确保没有其他 ERP 进程占用端口（见下文「端口被占用」处理）。

---

## 2. 如何启动 ERP

1. 打开 PowerShell 或 CMD。
2. 进入项目目录：
   ```
   cd /d C:\Users\Administrator\Documents\ERP
   ```
   （请按实际路径修改）
3. 编译：
   ```
   dotnet build ERP.csproj
   ```
4. 启动：
   ```
   .\bin\Debug\net8.0-windows\ERP.exe
   ```
5. 浏览器访问 `http://127.0.0.1:8787`，应看到登录页面（HTTP 200）。

**若启动失败**：

```
taskkill /F /IM ERP.exe
```

然后重新执行第 4 步。

---

## 3. 如何登录管理员

1. 打开 `http://127.0.0.1:8787`
2. 输入：
   - 账号：`admin`
   - 密码：`admin123`
3. 登录成功后，右上角应显示「系统管理员」或 admin 相关信息。
4. 左侧菜单应显示全部模块。

---

## 4. 数据目录验收

1. 访问 `GET /api/info`（登录后可在浏览器开发者工具 Network 中查看，或使用 curl）。
2. **预期**：`dataDir` 为 `D:\冠誉制造ERP\Data`。
3. 托盘菜单「打开数据目录」应打开 `D:\冠誉制造ERP\Data`。
4. 新增供应商、客户、物料后，分别确认 `suppliers.json`、`customers.json`、`materials.json` 写入 `D:\冠誉制造ERP\Data`。
5. 手动备份后，确认备份目录在 `D:\冠誉制造ERP\Backups`（不在旧 `C:\ProgramData\智造ERP供应商管理\backups`）。
6. 若旧目录 `C:\ProgramData\智造ERP供应商管理` 存在且新目录原先无 JSON，首次启动应自动迁移（不删除旧目录、不覆盖新目录已有文件），并在 `operation.log` 或控制台记录迁移信息。

---

## 5. 如何使用测试数据导入导出

**路径**：设置 → 系统设置 → **测试数据导入导出**

> 仅 admin 可见此入口。

页面包含以下按钮：

| 按钮 | 作用 |
|------|------|
| 一键导出测试总表 | 下载 Excel 备份文件 |
| 选择 Excel 文件 | 选择要导入的 xlsx 文件 |
| 预检查 | 解析并校验，不写入数据 |
| 开始导入 | 正式写入数据 |

---

## 6. 如何导出测试总表

1. admin 登录。
2. 进入「设置 → 测试数据导入导出」。
3. 点击「一键导出测试总表」。
4. 浏览器应下载文件，文件名格式：
   ```
   ERP测试数据总表yyyyMMdd.xlsx
   ```
   例如：`ERP测试数据总表20260624.xlsx`
5. 用 Excel 打开，确认包含以下 Sheet（有数据的模块会有内容，空模块可能只有表头）：
   - 供应商管理、客户管理、物料管理
   - BOM表、机型成本
   - 销售订单、销售出库
   - 采购单、采购入库
   - 生产领用、成品入库
   - 应收款、应付款
   - 库存汇总（参考 Sheet）

---

## 7. 如何预检查 Excel

1. 在测试数据页面点击「选择 Excel 文件」。
2. 选择刚导出的 xlsx 文件（或准备好的测试文件）。
3. 点击「预检查」。
4. 检查页面显示：
   - 当前文件名
   - 识别到的 Sheet 列表
   - 每个 Sheet 对应的模块名称
   - 每个模块的数据条数
   - 「库存汇总」应标记为「仅参考，不导入」
   - 预检查错误列表（无错误则显示「无」）

---

## 8. 如何正式导入

1. 预检查通过后，点击「开始导入」。
2. 确认弹窗提示，点击确定。
3. 等待导入完成，查看结果：
   - 每个模块：新增、更新、跳过、失败数量
   - 汇总：总新增、总更新、总跳过、总失败
   - 失败原因（如有）
4. 进入各业务模块，抽查数据是否正确。

---

## 9. 如何重复导入并检查防重复

1. **不要修改 Excel**，再次选择同一文件。
2. 先「预检查」，再「开始导入」。
3. 预期结果：
   - **不应产生大量重复新增**
   - 供应商/客户/物料：已存在的记录应显示「更新」或「跳过」
   - 单据类（销售订单、采购单等）：相同单号应「跳过」
4. 若出现大量新增，说明防重复逻辑异常，需排查。

---

## 10. 如何测试单模块导出

验证单模块 CSV 导出文件名规则：**模块名称 + yyyyMMdd + .csv**

| 模块 | 操作 | 预期文件名示例 |
|------|------|----------------|
| 供应商管理 | 点击「导出表格」 | `供应商管理20260624.csv` |
| 客户管理 | 点击「导出表格」 | `客户管理20260624.csv` |
| 物料管理 | 点击「导出表格」 | `物料管理20260624.csv` |
| BOM表 | 点击「导出BOM」 | `BOM表20260624.csv` |
| 机型成本 | 点击「导出机型成本」 | `机型成本20260624.csv` |
| 销售订单等 | 各模块导出按钮 | `模块名yyyyMMdd.csv` |

确认：

- 文件能正常打开，中文不乱码
- 表头与页面列表字段一致
- 测试总表导出（xlsx）与单模块导出（csv）互不影响

---

## 11. 如何测试普通用户权限

### 11.1 准备子账号

1. admin 登录 → 设置 → 系统设置 → 账号管理。
2. 新增子账号，例如：
   - 账号：`testuser`
   - 密码：自行设置
   - 勾选部分权限（如 `settings.view`、`supplier.view`）
3. 保存。

### 11.2 测试菜单可见性

1. 退出 admin，用 `testuser` 登录。
2. 进入「设置 → 系统设置」。
3. **预期**：看不到「测试数据导入导出」入口。
4. **预期**：看不到「数据维护」「字典选项」入口（若未授予管理员权限）。

### 11.3 测试 API 拒绝

用子账号登录后，在浏览器开发者工具或使用 curl 测试（应返回 **403**）：

| 接口 | 方法 |
|------|------|
| `/api/test-data/export-all` | GET |
| `/api/test-data/import-preview` | POST |
| `/api/test-data/import-run` | POST |

**预期错误信息**：`仅管理员可操作测试数据导入导出`

### 11.4 测试子账号正常功能

子账号应仍能使用已授权的功能，例如：

- 有 `supplier.view` 权限 → 可查看和导出供应商 CSV
- 有 `sales_order.view` 权限 → 可查看销售订单

---

## 12. 完整验收清单

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | `dotnet build ERP.csproj` | 成功，0 错误 |
| 2 | ERP 启动 | 托盘有图标，浏览器可访问 |
| 3 | HTTP 200 | `http://127.0.0.1:8787` 返回登录页 |
| 4 | admin 登录 admin/admin123 | ✅ |
| 5 | `GET /api/info` 的 dataDir | `D:\冠誉制造ERP\Data` |
| 6 | 新增供应商写入 suppliers.json | 在 `D:\冠誉制造ERP\Data` |
| 7 | 新增客户写入 customers.json | 在 `D:\冠誉制造ERP\Data` |
| 8 | 新增物料写入 materials.json | 在 `D:\冠誉制造ERP\Data` |
| 9 | admin 可导出测试总表 xlsx | ✅ |
| 10 | admin 可预检查 | ✅ |
| 11 | 重复导入不大量重复 | ✅ |
| 12 | 单模块 CSV 导出文件名正确 | ✅ |
| 13 | 子账号看不到测试数据入口 | ✅ |
| 14 | 子账号调用 test-data API 返回 403 | ✅ |
| 15 | JSON 数据结构未改变 | ✅ |
| 16 | 备份在 `D:\冠誉制造ERP\Backups` | ✅ |

---

## 13. 代码乱了如何用 Git 恢复

若本地代码改乱、需要恢复到远程干净版本：

```bash
git fetch origin
git reset --hard origin/main
```

**警告**：

- `git reset --hard` 会**永久丢弃**本地未 commit 的修改，执行前请确认不需要保留。
- 业务数据不在 Git 仓库中，恢复代码不会影响 `D:\冠誉制造ERP\Data` 里的运行数据。
- 若只需恢复某个文件：`git checkout origin/main -- 文件名`

---

## 相关文档

- [README.md](README.md) — 项目总览
- [安装说明.txt](安装说明.txt) — 安装步骤
- [使用教程.md](使用教程.md) — 日常操作
