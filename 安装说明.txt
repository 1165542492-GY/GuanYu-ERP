冠誉ERP管理系统 Ver2.8
========================

一、安装步骤
1. 解压“冠誉ERP管理系统Ver2.8.zip”到任意文件夹，例如 D:\冠誉ERP
2. 进入解压后的文件夹
3. 右键 ERP.exe，选择“以管理员身份运行”
4. 程序启动后会出现在 Windows 系统托盘，并自动打开浏览器

二、访问地址
- 本机访问：http://127.0.0.1:8787/
- 局域网访问：使用程序界面中显示的局域网地址
- 同一局域网内其他电脑可共用同一套数据

三、首次登录
- 管理员账号：admin
- 默认密码：admin123
- 首次启动且 users.json 不存在时，系统会自动创建系统管理员账号
- 修改密码后以实际设置为准，重启或升级不会恢复为 admin123
- 子账号由系统管理员在「设置 → 系统设置 → 账号管理」中新增并分配权限

四、数据保存位置
程序运行数据默认保存在：
C:\ProgramData\智造ERP供应商管理

该目录包含：
- 供应商、客户、物料、财务等业务 JSON 数据
- bom.json（BOM表）、model_costs.json（机型成本）、system_settings.json（系统税率）
- dictionary_options.json（字典选项，Ver2.7）
- sales_orders.json、sales_outbounds.json、purchase_orders.json、purchase_inbounds.json（Ver2.8）
- production_picks.json、finished_inbounds.json、receivables.json、payables.json（Ver2.8）
- contracts.json（合同）、contract_settings.json（合同资料）
- users.json 账号与权限数据（密码 SHA256 哈希存储）
- backups 备份目录
- operation.log 操作日志

升级、迁移或重装前，请先完整备份上述目录。

五、账号说明
- admin 为系统主账号，拥有全部权限，不可删除、不可禁用
- 子账号默认无业务权限，需管理员勾选后才能操作对应模块
- 无权限时菜单隐藏或提示「无权限操作」
- 字典选项（Ver2.7）仅 admin 可见、可操作；子账号在系统设置中看不到该入口
- Ver2.8 新增销售/采购/库存/应收应付等模块权限，子账号需管理员勾选

六、Ver2.8 新增功能

（1）主业务基础框架
新增并可用的菜单与模块：
- 销售管理：销售订单、销售出库
- 采购管理：采购单、采购入库
- 生产管理：生产领用、成品入库（BOM表、机型成本保留 Ver2.7 功能）
- 仓库管理：库存汇总（只读，不可手工新增库存）
- 财务管理：应收款、应付款（财务收支保留原有功能）

各模块支持新增、编辑、删除、查询。单据编号自动生成。金额保留 2 位小数。删除前需确认。弹窗点击空白处不会关闭。

（2）库存汇总第一版
仅统计「已确认」记录：
当前库存 = 采购入库已确认 + 成品入库已确认 - 销售出库已确认 - 生产领用已确认

（3）预留联动（本版未实现）
销售/采购订单与出入库自动下推、业务单据自动生成应收应付、库存品名与物料档案统一等，将在后续版本完善。

七、Ver2.8 建议测试步骤
1. admin 登录，进入销售订单，新增一条记录，确认金额=数量×单价
2. 分别新增采购入库、成品入库（状态选「已确认」），再新增销售出库、生产领用（已确认）
3. 打开库存汇总，确认数量计算正确；草稿状态记录不应计入
4. 应收款/应付款：输入金额，确认未收/未付自动计算
5. 测试删除确认与弹窗遮罩不可关闭
6. 回归 Ver2.7：删除链路、机型成本启停、BOM 弹窗、字典选项

（4）一键清空测试数据（仅 admin）
路径：设置 → 系统设置 → 数据维护
- 用于测试阶段快速清空业务测试数据
- 默认二次密码：88888888（与登录密码无关，保存在 system_settings.json）
- 清空前自动备份到 backups/backup_before_clear_年月日_时分秒/（完整数据清单，与手动备份一致）
- 须输入二次密码，并准确输入确认文字「确认清空」
- 会清空：客户、供应商、物料、BOM、机型成本、合同、财务、Ver2.8 全部业务 JSON
- 不会清空：账号、权限、系统设置、字典选项、合同资料模板
- 子账号看不到入口，也不能调用接口

八、Ver2.8 测试报告修复说明
1. 合同页面：修复 Business 资源注入破坏合同 JS 的问题，新增合同按钮可正常弹窗
2. 手动备份：系统设置 → 数据备份，备份至 backups/manual_backup_年月日_时分秒/，包含全部业务 JSON、用户、设置、字典、合同、操作日志
3. 清空前备份：与手动备份使用同一份完整文件清单；清空失败会自动恢复
4. 库存汇总：物料与成品按「类型+ID」分别统计，同名不再合并
5. 应收/应付状态：未收款、部分收款、已收清 / 未付款、部分付款、已付清
6. 数量字段在 Business 表格中按数字显示，金额字段显示 ¥
7. 非法数量、超额收付款返回 400/422 及中文 message

九、Ver2.7 新增功能

（1）删除链路保护
引用关系：供应商 → 物料 → BOM → 机型成本
- 供应商被物料引用时，不能删除供应商
- 物料被 BOM 引用时，不能删除物料
- BOM 被机型成本引用时，不能删除 BOM
删除失败时界面会显示后端返回的中文提示信息。

（2）机型成本启用 / 停用
- 列表显示「状态」列（启用 / 停用）
- 操作列可点击「启用」或「停用」切换状态
- 启用状态的记录不能删除，须先停用后再删除
- 导入、导出、模板、列表均包含状态字段
- 材料成本合计与平均成本仅统计启用记录（页面有说明）

（3）BOM 弹窗优化
- 明细表格可横向、纵向滚动，表头固定
- 「保存 BOM」与「新增明细行」在同一栏
- 新增明细行出现在表格顶部
- 物料下拉仅显示启用物料；编辑时已选物料仍显示
- 弹窗顶部显示当前系统税率及成本核算说明
- 不再显示「重新取最新物料价格」按钮

（4）字典选项
路径：设置 → 系统设置 → 字典选项（仅管理员）
- 维护客户分类、供应商分类、物料分类、收支分类、合同分类、订单分类等
- 支持新增、编辑、删除、启用、停用
- 可按分类类型、关键字、状态查询
- 客户/供应商/物料/财务页面的下拉联动将在后续版本逐步接入

九、Ver2.7 建议测试步骤
1. 用 admin 登录，分别测试供应商、物料、BOM、机型成本的删除保护（被引用时应失败并显示中文原因）
2. 在机型成本页面测试启用/停用按钮；启用状态下尝试删除应被拦截
3. 打开 BOM 新增/编辑弹窗，检查滚动、表头、按钮位置与新行位置
4. 进入字典选项，测试增删改与筛选；用子账号登录确认无字典入口
5. 导出/导入机型成本 CSV，确认含「状态」列

十、Ver2.6 新增功能（保留）
- 财务收支支持 CSV/XLSX 表格导入、模板下载和逐行错误反馈
- 所有页面顶部统一显示当前账号与权限
- 所有弹窗禁止点击外部空白处或按 ESC 自动关闭
- BOM 与机型成本按物料管理当前价格实时核算，缺价时显示警告
- BOM 已使用的物料禁止删除

十一、Ver2.5 新增功能（保留）
- 合同管理 → 合同资料、合同创建、预览与打印
- 合同金额自动计算与中文大写金额

十二、Ver2.4 / Ver2.3 功能（保留）
- BOM 表、机型成本 CSV 导入导出
- 生产管理 BOM 表与机型成本
- 系统设置 → 税率修改
- 物料管理「价格类型：含税 / 不含税」

十三、常见问题
1. 程序无法启动
   请确认是否已“以管理员身份运行”。

2. 局域网其他电脑无法访问
   请检查 Windows 防火墙是否允许端口 8787，并确认访问地址使用的是服务器电脑的局域网 IP。

3. 提示程序已经在运行
   说明系统已在运行，请查看系统托盘图标，或在任务管理器中结束旧进程后再启动。

4. 数据丢失风险
   程序运行期间不要手工编辑 JSON 数据文件；建议定期使用程序内的“立即备份”功能。

5. 删除提示看不懂
   Ver2.7 起删除失败会直接显示后端中文原因，请根据提示先解除引用或停用记录。

十四、版本说明
详细功能记录请查看同目录下的 VERSION.md。
