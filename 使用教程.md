# 冠誉ERP — 使用教程

> 本文面向日常操作人员，按菜单顺序说明常用功能。  
> 建议配合 [README.md](README.md) 与 [安装说明.txt](安装说明.txt) 一起阅读。

---

## 1. 登录系统

1. 确保**主电脑**已双击启动 `D:\冠誉制造ERP\App\冠誉制造ERP.exe`（或桌面快捷方式「冠誉制造ERP」），系统托盘有图标。
2. 程序启动后会**自动打开浏览器**；若未打开，请手动访问 `http://127.0.0.1:8787`。
3. **老婆/其他电脑**（同一局域网）在浏览器输入 `http://主电脑IP:8787`，例如 `http://192.168.1.28:8787`。
4. 输入账号和密码，点击登录。
5. 默认管理员：`admin` / `admin123`。
6. 登录成功后进入首页；右上角可看到当前账号名称。
7. 退出：点击右上角「退出登录」。

**多电脑使用**：只能在一台主机双击运行 `冠誉制造ERP.exe`；其他电脑**只用浏览器**访问主机 IP，不要在多台电脑各自运行一套 ERP。

---

## 2. 数据保存位置

ERP 正式业务数据固定保存在 D 盘：

| 目录 | 用途 |
|------|------|
| `D:\冠誉制造ERP\Data` | 业务 JSON 数据（供应商、客户、物料、订单、账号等） |
| `D:\冠誉制造ERP\Backups` | 自动备份、手动备份、清空前备份 |
| `D:\冠誉制造ERP\Exports` | 建议将浏览器导出的 CSV/Excel 保存到此目录 |
| `D:\冠誉制造ERP\Imports` | 建议将待导入的 Excel/CSV 文件放此目录 |

- 托盘菜单「打开数据目录」会打开 `D:\冠誉制造ERP\Data`。
- 旧版目录 `C:\ProgramData\智造ERP供应商管理` 仅作历史兼容，不再写入新数据。
- **不要把 ERP 数据目录放在 WPS 云盘、OneDrive、百度网盘等同步目录中**，避免同步软件与程序同时读写 JSON 导致损坏。

---

## 3. 首页说明

首页包含**经营看板**（上方）和**档案参考旧表**（下方）两部分：

- **经营看板**：销售额、采购额、财务收支等受右上角「统计范围」（全部/今日/本月/本年）影响；应收余额、应付余额、当前库存为累计或实时汇总，**不随日期范围变化**。
- 点击经营看板标题旁的 **「查看统计口径」** 可展开完整说明。
- **下方旧表**（应收明细、应付明细、库存物料）数据来自客户/供应商/物料**档案字段**，仅供快速查看；真实应收应付请用「财务管理 → 应收款/应付款」，真实库存请用「仓库管理 → 库存汇总」。

首页主要用于**快速了解现状**，具体业务操作请进入对应菜单模块。

---

## 3.1 业务单据「已确认」与删除注意

以下模块单据状态为 **已确认** 时，可能影响库存、应收或应付：

- 销售订单、销售出库、采购单、采购入库、生产领用、成品入库

**保存为已确认**时系统会二次确认；**删除已确认单据**前也会提示可能影响联动数据。

删除有关联数据的单据时请注意：

- 销售订单若已被销售出库引用，需先处理出库单。
- 采购单若已被采购入库引用，需先处理入库单。
- 应收款/应付款若已有收款/付款明细，需先删除明细。

---

## 3.2 收款/付款明细与财务收支

**路径**：财务管理 → 应收款 / 应付款 → 收款明细 / 付款明细

- 收款/付款明细用于**更新应收应付状态**（已收/未收、已付/未付）。
- **不会自动写入**「财务管理 → 财务收支」中的银行或现金流水。
- 如需记录实际银行/现金收支，请到 **财务收支** 模块单独登记。

---

## 3.3 备份恢复后必做操作

**路径**：设置 → 系统设置 → 数据备份恢复 → 恢复

恢复成功后请务必：

1. **关闭并重新启动** `冠誉制造ERP.exe`（或发布目录中的 exe）。
2. 在浏览器按 **Ctrl+F5** 强制刷新，避免看到旧缓存页面。

---

## 4. 资料管理

资料管理是 ERP 的基础，请先维护好供应商、客户、物料，再开展采购、生产、销售业务。

### 4.1 供应商管理

**路径**：资料管理 → 供应商管理

| 操作 | 步骤 |
|------|------|
| **新增** | 点击「＋ 新增供应商」→ 填写供应商名称（必填）及联系人、电话、地址等 → 保存 |
| **编辑** | 在列表中找到记录 → 点击「编辑」→ 修改后保存 |
| **删除** | 点击「删除」→ 确认。若该供应商已被物料引用，系统会提示不能删除 |
| **查询** | 在搜索框输入编号、名称、联系人、电话等关键字 → 回车或自动筛选 |
| **导出** | 点击「导出表格」，下载 CSV 文件，文件名格式：`供应商管理yyyyMMdd.csv` |
| **批量添加** | 点击「批量添加」→ 在表格中一次填写多行 →「保存全部」 |
| **导入** | 点击「导入表格」→ 选择 Excel 文件导入 |

编号说明：保存后系统自动生成，格式为 `GY` + 5 位流水号（如 `GY00001`）。

### 4.2 客户管理

**路径**：资料管理 → 客户管理

操作方式与供应商管理类似：

- **新增 / 编辑 / 删除 / 查询 / 导出 / 批量添加 / 导入**
- 导出文件名：`客户管理yyyyMMdd.csv`
- 编号格式：`KH` + 5 位流水号

**重要**：销售订单中的客户，必须来自此处已录入的客户。

### 4.3 物料管理

**路径**：资料管理 → 物料管理

| 操作 | 说明 |
|------|------|
| **新增** | 填写物料名称/规格（必填）、供应商、含税价/不含税价、价格类型等 |
| **编辑** | 修改价格、规格、状态等 |
| **删除** | 若物料已被 BOM 使用，系统会提示不能删除 |
| **查询** | 搜索编号、名称、供应商等 |
| **导出** | 文件名：`物料管理yyyyMMdd.csv` |

编号格式：`WL` + 5 位流水号。

---

## 5. 生产管理

### 5.1 BOM表 — 新增

**路径**：生产管理 → BOM表

1. 点击「＋ 新增 BOM」。
2. 填写机型编号、机型名称、产品名称、版本号等主表信息。
3. 在明细区域点击「＋ 新增明细行」。
4. 保存后系统自动生成 BOM 编号（`BOM` + 5 位流水号）。

### 5.2 BOM明细 — 如何选择物料

1. 在 BOM 弹窗的明细行中，点击「物料」下拉框。
2. 从「物料管理」中**已启用**的物料里选择。
3. 选择后自动带出物料编号、名称、规格、单价等信息。
4. 填写用量，系统自动计算该行金额及 BOM 总材料成本。
5. 可继续添加多行明细，保存 BOM。

### 5.3 机型成本 — 如何读取 BOM 成本

**路径**：生产管理 → 机型成本

1. 点击「＋ 新增机型成本」。
2. 选择关联的 BOM。
3. 系统自动带出机型编号、机型名称、产品名称及 **材料成本**（来自 BOM）。
4. 当前版本：总成本 = 材料成本。
5. 保存后可启用/停用；**启用状态的记录不能直接删除**，需先停用。

### 5.4 生产领用 — 如何记录

**路径**：生产管理 → 生产领用

1. 点击「新增生产领用」。
2. 可选择来源 BOM，填写领用物料、领用数量、成本单价。
3. 填写领用日期、状态（草稿 / 已确认 / 已取消）。
4. 保存后生成领用编号（`SCLL` + 5 位流水号）。
5. **已确认**的领用会参与库存汇总中的材料出库统计。

### 5.5 成品入库 — 如何记录

**路径**：生产管理 → 成品入库

1. 点击「新增成品入库」。
2. 可选择来源机型成本或 BOM，填写产品名称、入库数量、单台成本。
3. 填写入库日期、状态。
4. 保存后生成入库编号（`CPRK` + 5 位流水号）。
5. **已确认**的成品入库会参与库存汇总中的成品入库统计。

---

## 6. 采购管理

### 6.1 采购单 — 如何新增

**路径**：采购管理 → 采购单

1. 点击「新增采购单」。
2. 填写供应商名称（应使用供应商管理中已有的供应商）。
3. 选择或填写物料、数量、采购单价。
4. 金额 = 数量 × 单价，系统自动计算。
5. 选择订单日期、状态，保存。
6. 编号格式：`CGDD` + 5 位流水号。

### 6.2 采购入库 — 如何选择采购单

**路径**：采购管理 → 采购入库

1. 点击「新增采购入库」。
2. 在「来源采购单」下拉框中选择**已录入的采购单**。
3. 系统自动带出供应商、物料等信息。
4. 填写入库数量、入库单价、入库日期、状态。
5. 保存后生成入库编号（`CGRK` + 5 位流水号）。
6. **已确认**的采购入库会参与材料库存增加统计。

---

## 7. 销售管理

### 7.1 销售订单 — 如何新增

**路径**：销售管理 → 销售订单

1. 点击「新增销售订单」。
2. **客户**：必须从下拉框中选择「客户管理」里已有的客户，不能随意输入陌生客户。
3. 选择客户后，自动带出客户编号、名称、联系人等信息。
4. 填写物料、数量、含税/不含税销售单价。
5. 销售金额自动计算，选择订单日期、状态，保存。
6. 编号格式：`XSDD` + 5 位流水号。

### 7.2 销售出库 — 如何选择来源销售订单

**路径**：销售管理 → 销售出库

1. 点击「新增销售出库」。
2. 在「来源销售订单」下拉框中选择**已录入的销售订单**。
3. 系统自动带出客户、物料等信息。
4. 填写出库数量、成本单价、出库日期、状态。
5. 保存后生成出库编号（`XSCK` + 5 位流水号）。
6. **已确认**的销售出库会参与成品库存减少统计。

---

## 8. 仓库管理

### 8.1 库存汇总 — 怎么看

**路径**：仓库管理 → 库存汇总

- 本页面为**只读查询**，不能直接手工新增库存。
- 列表展示物料和成品的当前数量、成本价、库存金额等。
- 当前统计规则（仅「已确认」单据参与）：
  - 材料库存 ≈ 采购入库（已确认）− 生产领用（已确认）
  - 成品库存 ≈ 成品入库（已确认）− 销售出库（已确认）

### 8.2 使用说明

库存汇总目前主要用于**查看库存情况**，后续版本会逐步增强与采购、销售、生产的自动联动。

---

## 9. 财务管理

### 9.1 应收款 — 怎么看

**路径**：财务管理 → 应收款

- 记录客户应付款项：应收金额、已收金额、未收金额。
- 状态自动计算：未收款 / 部分收款 / 已收清。
- 可关联来源销售订单。
- 编号格式：`YS` + 5 位流水号。

### 9.2 应付款 — 怎么看

**路径**：财务管理 → 应付款

- 记录欠供应商款项：应付金额、已付金额、未付金额。
- 状态自动计算：未付款 / 部分付款 / 已付清。
- 可关联来源采购单。
- 编号格式：`YF` + 5 位流水号。

### 9.3 财务收支

**路径**：财务管理 → 财务收支

- 记录日常收付款流水、期初余额、账户余额等（原有功能）。

---

## 10. 合同管理（已删除）

合同模块已从本版删除，不参与发布。如需合同功能请参考历史版本文档。

---

## 11. 测试数据导入导出

**路径**：设置 → 系统设置 → **测试数据导入导出**

> ⚠️ **仅系统管理员（admin）可见此入口。** 普通子账号看不到，这是正常现象。

用途：测试阶段快速备份和恢复全部业务数据。

| 步骤 | 操作 |
|------|------|
| 1 | 点击「一键导出测试总表」→ 下载 `ERP测试数据总表yyyyMMdd.xlsx` |
| 2 | 点击「选择 Excel 文件」→ 选择刚导出的或准备好的 xlsx 文件（建议放在 `D:\冠誉制造ERP\Imports`） |
| 3 | 点击「预检查」→ 查看各 Sheet 识别情况、数据条数、错误提示 |
| 4 | 预检查完成后可点击 **「复制预检查结果」**，便于粘贴给同事或 AI 排查 |
| 5 | 确认无误后，点击「开始导入」→ 系统会先提醒**建议先备份** |
| 6 | 导入完成后可点击 **「复制导入结果」** 查看统计与失败原因 |

**注意**：

- **必须先预检查通过**（无失败行）才能正式导入。
- **正式导入前建议**在「数据备份恢复」中立即备份（导入非整单事务回滚）。
- Excel 总表中「库存汇总」Sheet **只作参考导出，不参与正式导入**。
- 再次导入同一文件时，重复编号会自动跳过或更新，不会产生大量重复数据。
- 不影响各模块单独导出的 CSV 文件。

---

## 12. 导出文件说明

| 导出类型 | 文件名规则 | 格式 | 建议保存位置 |
|----------|------------|------|--------------|
| 单模块导出（供应商、客户、物料、BOM 等） | 模块名称 + yyyyMMdd + 原扩展名 | `.csv` | `D:\冠誉制造ERP\Exports` |
| 测试数据总表导出 | ERP测试数据总表 + yyyyMMdd + .xlsx | `.xlsx`（多 Sheet） | `D:\冠誉制造ERP\Exports` |

示例：

- `供应商管理20260624.csv`
- `BOM表20260624.csv`
- `ERP测试数据总表20260624.xlsx`

---

## 13. 常见问题

### 页面没刷新 / 显示旧内容

按 **Ctrl + F5** 强制刷新浏览器页面。

### ERP 打不开 / 浏览器无法访问

1. 确认主电脑是否已双击 `D:\冠誉制造ERP\App\冠誉制造ERP.exe`（看系统托盘图标）。
2. 若提示端口 8787 已被占用，请先关闭已运行的 ERP 或其他占用该端口的程序。
3. 若未启动，可在源码目录执行 `发布冠誉制造ERP.bat` 重新发布，或运行 `bin\Debug\net8.0-windows\冠誉制造ERP.exe` 调试。
4. 若提示已在运行，先结束旧进程后再启动。

### 端口被占用

说明已有 ERP 进程在运行。用上面的 `taskkill` 命令结束旧进程后重启。

### 普通用户看不到「测试数据导入导出」

**正常现象。** 该功能仅系统管理员可用，普通子账号无入口。

### 删除失败提示看不懂

系统会显示中文原因，常见情况：

- 供应商被物料使用 → 先删除或修改相关物料
- 物料被 BOM 使用 → 先修改 BOM
- BOM 被机型成本使用 → 先删除或修改机型成本
- 机型成本为启用状态 → 先「停用」再删除

### 登录密码忘了

联系系统管理员（admin）在「账号管理」中重置子账号密码。admin 密码需通过 `D:\冠誉制造ERP\Data` 或 `D:\冠誉制造ERP\Backups` 中的备份恢复。

---

## 相关文档

- [安装说明.txt](安装说明.txt) — 安装与启动
- [测试说明.md](测试说明.md) — 功能测试步骤
- [README.md](README.md) — 项目总览
