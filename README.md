# 冠誉ERP管理系统

冠誉ERP管理系统是一套面向中小型制造企业的 Windows 局域网 ERP 管理程序。程序以 Windows 托盘应用运行，通过内置 HTTP 服务向同一局域网内的电脑提供浏览器管理界面。

- 当前版本：**Ver2.9-rc18-rc19**（表格显示 + 单据流转合并收口，开发版未同步正式 App）
- 运行平台：Windows
- 服务端口：`8787`
- 技术结构：C#/.NET 8、WinForms、`HttpListener`、原生 HTML/CSS/JavaScript

> **历史说明**：合同模块已从本版删除，不参与发布。

## Ver2.9-rc18-rc19 新增（合并收口）

- **rc18 表格显示**：通用 col-money / col-qty / col-action / table-cell-ellipsis；常用列靠前；长文本省略悬停
- **rc19 单据流转**：销售订单「转出库」→ 销售出库自动带出；采购单「转入库」→ 采购入库自动带出
- 可出库/可入库数量默认预填；无可转数量人话提示；库存不足仍只提醒不拦截
- **短期停点**：本轮验收通过后暂停 rc20 及之后主线，准备 Ver2.9 阶段性正式试运行
- 未重构库存/应收/应付/财务核心

## Ver2.9-rc17（已完成）

- tag `v2.9.0-rc17`；已 commit/push、正式 App 已同步、8787 验收通过
- 库存类型阶段 2：筛选、导出、提示文案、物料列表收口

## Ver2.8-rc3.6 新增

- **应付款/应收款删除保护**：自动/关联单据生成的应收应付禁止直接删除；有收付款明细或已收/已付金额时后端 409 拦截
- **收付款明细删除重算**：删除最后一条收款/付款明细后，已收/已付金额归零并重算状态
- **生产领用确认**：已确认领用不能重复确认；库存不足确认 409 拦截
- 修复来源于 rc3.6-dev 定点复测，rc3.6 定点复测 10/10 PASS

## Ver2.8-rc3.5 新增

- **操作影响预检**：删除、确认、编辑、清空、恢复等关键操作前，系统提前显示影响范围；不可继续的操作直接拦截
- **前后端双重校验**：前端弹窗提示 + 后端真实拦截（不依赖前端 alone）
- **统一确认弹窗**：沿用 rc3.4 风格，blocked 时仅显示「我知道了」
- 覆盖库存、应收、应付、BOM、机型成本、权限、数据维护等模块

## 已有功能

- 用户登录、退出及会话管理
- 系统管理员账号与子账号权限管理（Ver2.2）
- 生产管理：BOM表、机型成本（Ver2.3）；BOM/机型成本 CSV 导入导出（Ver2.4）
- 系统设置税率修改（Ver2.3）
- 首页应收、应付、库存物料和财务收支综合看板
- 供应商、客户、物料档案的新增、查询、修改、删除、Excel 导入和 CSV 导出
- 供应商、客户、物料的批量添加与批量删除
- 显示大小切换（小 / 标准 / 大 / 超大），设置保存在浏览器本地
- 宽表格顶部横向滚动条，与底部滚动条同步
- 财务收支流水、期初余额、账户余额、年度/月度筛选和用途统计
- JSON 数据持久化、修改前自动备份、手动备份和操作日志
- **数据备份恢复**（系统设置 → 数据备份恢复）：一键备份、从备份恢复；恢复成功后需重启 ERP 并 Ctrl+F5 刷新
- **测试数据导入导出**：须先预检查通过再正式导入；导入前建议先备份；支持复制预检查/导入结果文本
- **首页经营看板口径说明**：卡片标注累计全期/实时汇总；可展开「查看统计口径」；下方旧表为档案参考
- **收款/付款明细**仅更新应收应付状态，**不会自动写入**「财务管理 → 财务收支」流水
- 供应商、客户和物料编号自动生成
- Windows 托盘运行及局域网共享访问

## 左侧菜单结构

```text
首页

资料管理
  供应商管理、客户管理、物料管理、个性化设置（占位）

销售管理
  销售订单、销售出库

采购管理
  采购单、采购入库

仓库管理
  库存汇总

生产管理
  BOM表、机型成本、生产领用、成品入库

财务管理
  财务收支、应收款、应付款

设置
  系统设置（含字典选项、数据维护、测试数据导入导出，仅管理员）
```

Ver2.8 已实现销售、采购、生产领用/成品入库、库存汇总、应收款、应付款基础 CRUD 页面与接口。个性化设置仍为占位入口。

## Ver2.8

### 主业务基础框架（第一阶段）

不做复杂总账、复杂审批、复杂库存加权核算。本版本搭建销售、采购、库存、应收、应付及生产领用/成品入库的基础页面、模型、接口与 CRUD。

| 模块 | 菜单路径 | API |
|------|----------|-----|
| 销售订单 | 销售管理 → 销售订单 | `/api/sales-orders` |
| 销售出库 | 销售管理 → 销售出库 | `/api/sales-outbounds` |
| 采购单 | 采购管理 → 采购单 | `/api/purchase-orders` |
| 采购入库 | 采购管理 → 采购入库 | `/api/purchase-inbounds` |
| 生产领用 | 生产管理 → 生产领用 | `/api/production-picks` |
| 成品入库 | 生产管理 → 成品入库 | `/api/finished-inbounds` |
| 库存汇总 | 仓库管理 → 库存汇总 | `GET /api/stocks`、`GET /api/stocks/summary` |
| 应收款 | 财务管理 → 应收款 | `/api/receivables` |
| 应付款 | 财务管理 → 应付款 | `/api/payables` |

### 基础计算规则

- 销售订单金额 = 数量 × 销售单价
- 采购单金额 = 数量 × 采购单价
- 销售出库成本金额 = 出库数量 × 成本单价
- 采购入库金额 = 入库数量 × 入库单价
- 生产领用成本金额 = 领用数量 × 成本单价
- 成品入库金额 = 入库数量 × 单台成本
- 应收未收金额 = 应收金额 − 已收金额
- 应付未付金额 = 应付金额 − 已付金额
- 库存汇总（第一版，只读）：当前库存 = 采购入库已确认 + 成品入库已确认 − 销售出库已确认 − 生产领用已确认

单据状态：草稿 / 已确认 / 已取消。金额保留 2 位小数；删除前确认；弹窗点击空白处不关闭。

### 数据文件（Ver2.8 新增）

`sales_orders.json`、`sales_outbounds.json`、`purchase_orders.json`、`purchase_inbounds.json`、`production_picks.json`、`finished_inbounds.json`、`receivables.json`、`payables.json` 及对应 `*_sequence.json`

### 权限（子账号需在账号管理中勾选）

`sales_order.*`、`sales_outbound.*`、`purchase_order.*`、`purchase_inbound.*`、`production_pick.*`、`finished_inbound.*`、`stock.view`、`receivable.*`、`payable.*`

### 测试步骤（Ver2.8）

1. admin 登录，依次进入 9 个新模块，测试新增、编辑、删除、搜索
2. 销售订单：输入数量与单价，保存后确认金额自动计算
3. 采购入库、销售出库、生产领用、成品入库：分别新增草稿与已确认记录
4. 打开库存汇总，确认只统计已确认记录，草稿不参与计算
5. 应收款/应付款：输入应收/应付与已收/已付，确认未收/未付与状态（未收款/部分收款/已收清、未付款/部分付款/已付清）自动计算
6. 删除操作应弹出确认；弹窗点击遮罩不应关闭
7. 验证 Ver2.7 功能未受影响：删除链路、机型成本启停、BOM 弹窗、字典选项

### Ver2.8 测试报告修复（本版补丁）

1. **手动备份完整范围**：`POST /api/backup` 备份至 `backups/manual_backup_yyyyMMdd_HHmmss/`，包含用户、权限、系统设置、字典、客户、供应商、物料、财务、BOM、机型成本、Ver2.8 全部业务 JSON 及操作日志。
2. **清空前备份**：与手动备份使用同一份完整文件清单；清空写入先落临时目录，全部成功后再原子替换；失败时自动从备份恢复。
3. **库存汇总**：按「业务类型 + 稳定 ID」汇总（物料用 MaterialId，成品用 ModelCostId/BomId），同名物料与成品不再合并；返回 ItemType、ItemId、ItemCode、ItemName、Spec、Unit、WarehouseName、CurrentQuantity、CostPrice、StockAmount。
4. **业务来源关联**：采购入库/销售出库/生产领用/成品入库/应收/应付保存来源单据 ID 及关联字段；前端选择来源后自动带出。
5. **应收/应付状态文案**：未收款 / 部分收款 / 已收清；未付款 / 部分付款 / 已付清。
6. **Business 表格显示**：数量字段按普通数字显示，金额/单价/成本字段显示 ¥。
7. **业务校验**：非法数量、超额收付款返回 400/422 及 `{ message: "中文原因" }`，前端优先显示 `message`。

### 一键清空测试数据（仅 admin）

路径：设置 → 系统设置 → **数据维护**

- 用于测试阶段快速清空业务测试数据，避免逐条删除
- **默认二次密码**：`88888888`（与 ERP 登录密码无关，保存在 `system_settings.json` 的 `ClearDataPassword` 字段）
- 清空前自动备份到 `backups/backup_before_clear_yyyyMMdd_HHmmss/`（**完整数据清单**，与手动备份范围一致）
- 双重确认：第一层 confirm 提示 → 第二层输入二次密码 + 确认文字「确认清空」
- **会清空**：客户、供应商、物料、BOM、机型成本、财务收支/期初、Ver2.8 全部业务 JSON
- **不会清空**：users.json、system_settings.json（含二次密码）、dictionary_options.json
- 子账号无入口；调用 `POST /api/admin/clear-test-data` 非 admin 返回 403

### 清空全部业务数据（仅 admin）

路径：设置 → 系统设置 → **数据维护**

- 清空供应商、客户、物料、订单、库存、应收、应付、BOM、机型成本、财务等业务 JSON
- 清空前自动备份到 `backups/backup_before_clear_yyyyMMdd_HHmmss/`
- 须输入二次密码 + 确认文字「确认清空全部业务数据」
- **保留**：admin、用户、权限、系统设置、二次密码、字典、税率、编号规则、**操作记录**、**历史备份**
- 接口：`POST /api/admin/clear-all-business-data`

### 深度初始化空库（仅 admin）

路径：设置 → 系统设置 → **数据维护**

- 用于交付前、测试环境重置、准备干净空库
- 在「清空全部业务数据」基础上， additionally 清空：**操作记录**（保留一条初始化记录）、**备份恢复记录**、**历史备份文件夹**（默认保留本次最终备份）、**测试与导入导出痕迹**
- 执行前自动创建最终备份 `backups/backup_before_deep_initialize_yyyyMMdd_HHmmss/`；**备份失败禁止继续**
- 须输入二次密码 + 确认文字「我确认深度初始化空库」；点击后还有一次确认弹窗；接入操作影响预检
- **保留**：admin、用户账号、权限、系统设置、二次密码、字典、税率、编号规则、数据/备份目录配置
- **不会**重置 admin、不会删除用户与权限配置
- 接口：`POST /api/admin/deep-initialize`

请求体示例：

```json
{
  "password": "二次密码",
  "confirmText": "我确认深度初始化空库",
  "keepFinalBackup": true,
  "clearOldBackupFiles": true,
  "clearOperationLogs": true,
  "clearBackupRecords": true,
  "clearTestArtifacts": true
}
```

### 测试数据导入导出（长期保留，仅 admin）

路径：设置 → 系统设置 → **测试数据导入导出**

系统长期保留功能，用于测试阶段的**数据备份、恢复与回归测试**（非临时调试代码）。

- **权限**：仅系统管理员（admin）可见入口、可操作；子账号无菜单入口，强行访问会提示无权限并返回首页；相关 API 非 admin 返回 403
- **一键导出**：下载 `ERP测试数据总表yyyyMMdd.xlsx`（多 Sheet Excel，含供应商、客户、物料、BOM、机型成本、销售/采购/生产/应收应付及库存汇总）
- **导入**：选择 Excel 总表 → 预检查 → 开始导入；按业务依赖顺序写入；重复编号自动跳过或更新
- **库存汇总**：仅作为参考导出，**不参与正式导入**
- **接口**：`GET /api/test-data/export-all`、`POST /api/test-data/import-preview`、`POST /api/test-data/import-run`
- 不影响各模块原有单页 CSV 导出（文件名规则仍为「模块名 + yyyyMMdd + 扩展名」）

### 预留联动（本版未实现）

- 销售订单 ↔ 销售出库自动下推
- 采购单 ↔ 采购入库自动下推
- 业务单据 ↔ 应收/应付款自动生成
- 库存汇总 ↔ 物料档案品名统一
- 客户/供应商/物料下拉与字典选项联动

## Ver2.7

### 删除链路保护

删除顺序与引用关系：**供应商 → 物料 → BOM → 机型成本**。下游有引用时禁止删除（单条与批量），后端返回中文提示，前端直接显示 `message`：

| 操作 | 被引用时提示（示例） |
|------|----------------------|
| 删除供应商 | 该供应商已被物料使用，不能删除 |
| 删除物料 | 该物料已被 BOM 使用，不能删除 |
| 删除 BOM | 该 BOM 已被机型成本使用，不能删除 |

### 机型成本启用 / 停用

- 列表新增「状态」列（启用 / 停用）
- 操作列提供「启用」或「停用」按钮；启用时显示「停用」，停用时显示「启用」
- 启用状态的记录**不能删除**；须先停用。前端与后端均校验，提示：「请先停用后再删除」
- CSV 导入、导出、模板及列表均包含状态字段
- 材料成本合计、平均成本**仅统计启用记录**（页面卡片与说明文字已标注）

### BOM 弹窗优化

- 明细表格支持横向、纵向双滚动，表头固定
- 「保存 BOM」与「新增明细行」同一工具栏，便于操作
- 新增明细行插入**顶部**
- 物料下拉仅显示启用物料；编辑已有 BOM 时已选物料仍保留显示
- 弹窗顶部显示当前系统税率及成本核算说明
- 已移除「重新取最新物料价格」按钮；成本按后端实时取价逻辑处理

### 字典选项（系统设置）

路径：设置 → 系统设置 → **字典选项**（**仅系统管理员**可见、可操作）

- 支持分类：客户分类、供应商分类、物料分类、收支分类、订单分类、其他业务分类等
- 字段：分类类型、字典名称、字典值、排序、状态（启用/停用）、备注
- 支持新增、编辑、删除、启用、停用；删除前确认；失败时显示后端中文 message
- 查询：分类类型、关键字、状态
- 数据文件：`dictionary_options.json`
- 权限：管理员专属；子账号无入口
- 客户 / 供应商 / 物料 / 财务页面已预留 `bind*CategorySelect` 函数，后续版本逐步接入下拉

### 测试步骤（Ver2.7）

1. 以 **admin** 登录，进入供应商 / 物料 / BOM / 机型成本，验证删除链路：被引用时删除失败且提示为中文后端 message
2. 机型成本：列表查看状态列；点击启用/停用并刷新；启用状态尝试删除应被拦截
3. BOM：打开新增/编辑弹窗，验证双滚动、表头固定、保存按钮位置、新行在顶部、仅启用物料可选
4. 系统设置 → 字典选项：新增/编辑/启停/删除字典项；按分类、关键字、状态筛选
5. 以子账号登录：字典选项入口应隐藏；无权限模块提示「无权限操作」

## Ver2.6 修复与增强

- 财务收支支持 CSV/XLSX 导入及 CSV 模板下载；错误行不会阻断正确行导入
- 页面公共头部显示当前账号与权限，侧栏不再放置账号角色信息
- 所有弹窗只能通过关闭、取消或右上角 X 关闭
- BOM 和机型成本使用物料管理当前价格实时重算，仅包含物料用量成本
- 物料未设置价格时显示警告；已被 BOM 使用的物料禁止删除

## 合同管理（Ver2.5，已删除）

合同模块已从本版删除，不参与发布。历史版本曾提供合同资料、合同创建、预览与打印功能。

## 生产管理（Ver2.3）

路径：生产管理 → BOM表 / 机型成本

### BOM表

- 维护产品 / 机型的物料清单，自动计算材料成本
- BOM 明细从「物料管理」选择物料，读取当前最新单价与价格类型
- 价格类型为「含税」时，按系统税率换算不含税单价：`不含税单价 = 含税单价 ÷ (1 + 税率 / 100)`
- 价格类型为「不含税」时，直接使用不含税价
- BOM 列表与机型成本按物料管理当前价格实时重算，物料涨跌后金额同步变化
- 价格为空或 0 时显示价格缺失警告，允许保存但不会静默产生错误成本
- 保存时锁定明细税率，已保存 BOM 不受后续税率修改影响

### 机型成本

- 选择 BOM 后自动带出机型信息与材料成本
- 机型成本当前仅统计 BOM 材料成本，总成本 = 材料成本
- 材料成本来自 BOM 用量乘以物料管理当前价格，不包含人工、制造、运费等附加费用

### BOM / 机型成本 CSV 导入导出（Ver2.4）

- BOM 表：导出BOM、导入BOM、下载导入模板（CSV）
- 机型成本：导出机型成本、导入机型成本、下载导入模板（CSV）
- 导入前确认，导入后显示成功/失败统计与明细
- 权限：`bom.export` / `bom.import`、`model_cost.export` / `model_cost.import`

## 个性化设置

路径：资料管理 → 个性化设置

- **显示大小**：标准 / 大 / 超大，与右上角下拉框同步
- **主题颜色**：默认蓝、商务灰、护眼绿、紫色
- **表格密度**：紧凑、标准、宽松
- **默认打开页面**：登录后自动进入所选页面
- **删除确认开关**：默认开启，关闭后删除不再弹确认框

设置保存在浏览器 localStorage，刷新后保持。

## 系统设置

路径：设置 → 系统设置

包含 7 个入口：账号管理、修改密码、数据备份、数据恢复、系统参数、税率修改、**字典选项（仅管理员）**。

**Ver2.2 已实现：**

- **账号管理**：系统管理员（admin）可新增、编辑、删除子账号，并为子账号勾选模块权限
- **修改密码**：当前登录用户可修改自己的密码（需具备相应权限；admin 默认拥有）

**Ver2.3 已实现：**

- **税率修改**：设置系统默认税率（默认 10%），用于 BOM 含税价换算不含税价；保存到 `system_settings.json`

**Ver2.7 已实现：**

- **字典选项**：系统管理员维护基础字典（分类、名称、值、排序、状态、备注）；数据保存在 `dictionary_options.json`；子账号不可见

数据备份、数据恢复、系统参数仍为占位入口，后续版本逐步实现。

## 账号与权限（Ver2.2）

### 主账号

- 登录账号：`admin`
- 显示名称：系统管理员
- 默认密码：`admin123`
- 角色：系统管理员，拥有全部权限
- 首次启动且 `users.json` 不存在时，系统自动创建该主账号
- 若 `users.json` 已存在，程序不会覆盖；修改密码后以实际设置为准

### 子账号

- 由系统管理员在「设置 → 系统设置 → 账号管理」中新增
- 默认角色：普通用户
- 默认无业务权限，需管理员手动勾选
- 权限控制供应商、客户、物料、财务收支、BOM表、机型成本及系统设置相关权限

### 账号数据

账号信息保存在数据目录下的 `users.json`，密码以 SHA256 哈希存储，不会提交到 Git 仓库。

## 项目文件

- `Program.cs`：程序入口、HTTP 服务、接口、数据存储和备份逻辑
- `App.html`：主界面、登录和供应商管理
- `Customer.html`：客户管理界面
- `Material.html`：物料管理界面
- `Finance.html`：财务收支界面
- `app.manifest`：Windows 管理员权限及系统兼容性声明
- `ERP.csproj`：.NET 8 WinForms 项目文件（主项目，使用 `dotnet build` 编译）
- `build.ps1`：一键编译并生成安装包（基于 `ERP.csproj` / `dotnet build`）
- `GuanYuERP.csproj`：旧版 .NET Framework 4.0 项目文件，保留作参考，不作为主构建入口
- `安装说明.txt`：随安装包分发的用户使用说明

程序运行时会将四个 HTML 文件作为嵌入资源，由 `Program.cs` 组合后提供给浏览器。

## 编译方式

安装 .NET 8 SDK 后，在仓库根目录执行：

```powershell
dotnet build
```

项目会按 `ERP.csproj` 编译为 .NET 8 Windows WinForms 程序，输出 **`冠誉制造ERP.exe`**，HTML 文件作为嵌入资源打包。

## 发布 Windows 双击启动版（推荐）

在仓库根目录双击或执行：

```text
发布冠誉制造ERP.bat
```

脚本会清理并发布自包含 win-x64 版本到 `D:\冠誉制造ERP\App\冠誉制造ERP.exe`，并创建桌面快捷方式。

## 一键打包（推荐）

在源码目录 `01-源代码` 中执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build.ps1
```

脚本会使用 `dotnet build ERP.csproj -c Release` 编译，生成 `ERP.exe`，并打包到 `02-安装包` 目录。`GuanYuERP.csproj` 为旧版 .NET Framework 项目，不再作为打包入口。

## 运行方式（Windows 双击启动版）

### 正式发布目录

执行项目根目录下的 **`发布冠誉制造ERP.bat`**，会将自包含 Windows 版本发布到：

```text
D:\冠誉制造ERP\App\冠誉制造ERP.exe
```

发布脚本还会在当前用户桌面创建快捷方式 **`冠誉制造ERP.lnk`**（指向上述 exe）。

### 日常使用

1. **主电脑**双击 `D:\冠誉制造ERP\App\冠誉制造ERP.exe`（或桌面快捷方式）。
2. 程序启动后驻留系统托盘，**自动打开浏览器**访问 `http://127.0.0.1:8787/`（仅启动时打开一次）。
3. Web 服务默认监听 **`http://0.0.0.0:8787`**，局域网内其他电脑可通过 `http://主机IP:8787` 访问（例如 `http://192.168.1.28:8787`）。
4. 若 8787 端口已被占用，程序会弹出明确提示，不会直接闪退。
5. 程序需要管理员权限（清单已声明）；启动失败时请右键「以管理员身份运行」。

### 多电脑使用规则

- **只能在一台主机运行 `冠誉制造ERP.exe`**；妻子/同事/其他电脑**只用浏览器**访问主机 IP，不要在多台电脑各自运行一套 ERP。
- 所有电脑共用主机 `D:\冠誉制造ERP\Data` 中的数据。

### 开发者编译（源码调试）

安装 .NET 8 SDK 后，在仓库根目录执行 `dotnet build`，输出为 `冠誉制造ERP.exe`（与发布版同名）。

首次登录默认管理员账号为 `admin`，密码为 `admin123`。

## 数据目录

ERP 正式业务数据固定保存在 D 盘，便于备份、迁移和查找：

```text
D:\冠誉制造ERP\Data          ← 业务 JSON 数据（users.json、suppliers.json 等）
D:\冠誉制造ERP\Backups       ← 自动备份、手动备份、清空前备份
D:\冠誉制造ERP\Exports       ← 建议将浏览器导出的 CSV/Excel 保存到此目录
D:\冠誉制造ERP\Imports       ← 建议将待导入的 Excel/CSV 文件放此目录
```

`Data` 目录中包含供应商、客户、物料、财务和编号序列等 JSON 数据，以及：

- `users.json`：登录账号与权限（密码 SHA256 哈希存储）
- `bom.json`、`model_costs.json`、`system_settings.json`、`dictionary_options.json`：BOM、机型成本、系统税率、字典选项
- `operation.log`：操作审计日志

`Backups` 目录包含自动备份、手动备份（`manual_backup_yyyyMMdd_HHmmss/` 完整目录）及清空前备份（`backup_before_clear_yyyyMMdd_HHmmss/`）。

系统最多保留 50 个 JSON 备份文件。升级或迁移前，请完整备份 `D:\冠誉制造ERP` 目录。

### 测试沙盒数据目录（Ver2.9-rc19.1）

**正式日常使用不要设置沙盒。** 默认数据目录始终为 `D:\冠誉制造ERP\Data`。

仅在开发/自动化测试需要「空白新系统」或隔离造数时，可显式指定沙盒目录。

**注意**：程序需管理员权限运行。`ERP_DATA_DIR` 请在**已提升的管理员命令行**中设置，或写入用户级环境变量后再启动；从普通 PowerShell 会话仅设置 `$env:ERP_DATA_DIR` 再启动，子进程可能读不到该值。推荐测试时使用 `--data-dir` 启动参数（优先级更高、最可靠）。

**方式 1 — 环境变量**

```powershell
$env:ERP_DATA_DIR = "D:\冠誉制造ERP\TestData\E2E_BlankMachineFactory_20260628_160000"
.\bin\Debug\net8.0-windows\冠誉制造ERP.exe
```

**方式 2 — 启动参数（优先于环境变量）**

```powershell
.\bin\Debug\net8.0-windows\冠誉制造ERP.exe --data-dir "D:\冠誉制造ERP\TestData\E2E_BlankMachineFactory_20260628_160000"
```

- 优先级：`--data-dir` > `ERP_DATA_DIR` > 默认 `D:\冠誉制造ERP\Data`
- 沙盒目录不存在时会自动创建；沙盒默认为空白，**不会**复制正式 Data
- 禁止将磁盘根目录、`D:\冠誉制造ERP\App`、`Backups` 或正式 `Data` 设为沙盒路径
- 验证当前目录：`GET http://127.0.0.1:8787/api/system/data-dir`（无需登录）
- **空白新系统机床厂全流程测试必须在沙盒下执行**

### 旧数据目录（历史兼容）

旧版数据目录 `C:\ProgramData\智造ERP供应商管理` **不再作为正式数据目录**。首次启动时，若新目录尚无 JSON 数据且旧目录存在，程序会自动将旧目录下的 JSON 复制到 `D:\冠誉制造ERP\Data`（不删除、不覆盖旧数据）。

### 多电脑与云盘注意事项

- **不要把 ERP 数据目录放在 WPS 云盘、OneDrive、百度网盘等同步目录中**，同步软件可能与程序同时读写 JSON，导致数据损坏。
- **多电脑使用时，只能在一台主机运行 `冠誉制造ERP.exe`**；其他电脑通过浏览器访问主机 IP（如 `http://192.168.x.x:8787`），共用主机上的 D 盘数据。

## 批量操作与显示设置

### 批量删除

在供应商、客户、物料管理页面：

1. 勾选表格左侧复选框，或使用表头全选
2. 点击「批量删除」
3. 确认本次将删除的数据条数

未选择数据时会提示「请先选择要删除的数据」。

### 批量添加

1. 点击「批量添加」打开批量录入窗口
2. 在表格中一次填写多行资料，可继续添加行或删除未保存的临时行
3. 点击「保存全部」统一校验并保存

批量添加采用部分成功策略：正确的数据会保存，重复或错误的数据会跳过，并在保存后显示跳过原因。若全部数据均有误，则不会保存任何数据。

Excel 导入功能保持不变。

### 显示大小

顶部栏提供「显示大小」切换：标准 / 大 / 超大。选择后会保存在浏览器 `localStorage` 中，下次打开仍保持该设置。

### 表格横向滚动

供应商、客户、物料的宽表格在顶部和底部均提供横向滚动条，拖动任一侧滚动条时，表格内容会同步左右移动。

## 注意事项

- 程序清单要求管理员权限；启动失败时请确认是否以管理员身份运行。
- 服务监听局域网端口 `8787`，请按实际网络环境配置 Windows 防火墙。
- 当前使用 HTTP 明文通信，建议仅在可信局域网内使用，不要直接暴露到公网。
- 数据由运行程序统一读写，不建议在程序运行时手工编辑 JSON 文件。
- 正式数据在 `D:\冠誉制造ERP\Data`，请勿放入云盘同步目录；多电脑共用数据时只在一台主机运行 ERP。
- 数据文件、备份、日志、编译产物和安装包 zip 不应提交到 Git 仓库。
- 安装包默认输出到仓库外的 `02-安装包` 目录，不会进入 GitHub。
- 账号数据保存在数据目录的 `users.json` 中，密码以 SHA256 哈希存储；权限同时控制菜单、按钮和后端接口。

详细版本功能记录请参阅 [VERSION.md](VERSION.md)。
