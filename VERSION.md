# 版本记录

## Ver2.7

当前代码版本：冠誉ERP管理系统 Ver2.7。

### 删除链路保护

引用链：**供应商 → 物料 → BOM → 机型成本**。下游有引用时禁止删除（单条与批量），返回明确中文提示：

- 供应商被物料引用 → 不能删除供应商
- 物料被 BOM 引用 → 不能删除物料
- BOM 被机型成本引用 → 不能删除 BOM

### 机型成本启用 / 停用

- 机型成本新增「启用 / 停用」状态；列表、弹窗、CSV 导入导出均支持
- 列表操作列提供启用/停用切换；切换成功后刷新列表
- **启用状态不能删除**，须先停用（前端与后端双重校验）
- 材料成本合计、平均成本仅统计**启用**记录

### BOM 弹窗优化

- 明细表双滚动（横/纵）、表头 sticky 固定
- 「保存 BOM」与「新增明细行」同一栏
- 新明细行插入顶部；物料下拉仅启用物料，已选物料编辑时保留
- 弹窗显示当前系统税率与成本核算说明
- 移除「重新取最新物料价格」按钮；成本按物料管理当前价实时核算

### 字典选项

- 系统设置新增「字典选项」入口，**仅系统管理员**可见可操作
- 支持分类维护：客户/供应商/物料/收支/合同/订单/其他业务等
- 字段：分类类型、名称、值、排序、状态、备注
- 增删改、启用/停用、分类/关键字/状态查询
- 删除失败显示后端中文 message；已被业务使用时建议停用
- 数据文件：`dictionary_options.json`；权限键 `settings.dictionary`（管理员默认拥有）
- 客户/供应商/物料/财务页面预留字典下拉接入函数（Ver2.8 逐步联动）

### 管理员权限说明

- 字典选项：仅 `admin`（IsAdmin）可见入口并可操作 API
- 子账号：系统设置中无字典选项入口；调用接口返回 403
- 其他 Ver2.7 功能按既有模块权限（bom.*、model_cost.*、supplier.delete 等）控制

### 测试步骤

1. admin 登录，验证删除链路四类场景的中文错误提示
2. 机型成本启停、启用态不可删除、统计卡片「仅统计启用记录」
3. BOM 弹窗 UI 与物料过滤
4. 字典选项 CRUD 与筛选；子账号无入口
5. CSV 导入导出含机型成本状态列

## Ver2.6

当前代码版本：冠誉ERP管理系统 Ver2.6。

- 财务收支新增 CSV/XLSX 导入及 CSV 模板下载，支持逐行校验、部分成功和失败行原因
- 公共页头统一显示当前账号与权限，增加“小”显示档并修复超大模式侧栏重叠
- 所有业务弹窗禁止点击遮罩或按 ESC 关闭
- BOM 与机型成本按物料管理当前价格实时核算，仅统计物料用量成本
- 物料价格为零或缺失时，在 BOM 和机型成本页面显示明确警告
- 后端禁止删除已被 BOM 使用的物料，单条与批量删除均执行强校验
- 新增权限键：`finance.import`

## Ver2.5

当前代码版本：冠誉ERP管理系统 Ver2.5。

### 合同管理

- 新增左侧菜单分组「合同管理」，包含：合同、合同资料
- **合同资料**：支持 18 类资料类型的新增、编辑、删除、搜索、启用/停用
- 系统首次运行或合同资料为空时，预置「冠誉设备购销合同默认范本」
- **合同**：新增、编辑、删除、搜索、复制、作废、预览、打印
- 支持设备明细、随机配件、设备配置表、技术参数表动态增删行
- 支持从合同资料引用付款方式、条款、配件、配置项等，保存时写入快照
- 合同范本支持 HTML 占位符替换生成预览
- 金额自动计算：明细合计、定金、余款、分期金额；中文大写金额
- 数据文件：`contracts.json`、`contract_settings.json`
- 权限键：`contract.view/add/edit/delete/preview/print`、`contract_setting.view/add/edit/delete`

## Ver2.4

当前代码版本：冠誉ERP管理系统 Ver2.4。

### BOM 表导入导出

- BOM 表支持导出当前列表为 CSV（`BOM表_yyyyMMdd_HHmmss.csv`），含主表与明细扁平化字段
- BOM 表支持从 CSV 导入，按 BOM编号 + BOM版本 判断新增或更新
- 导入冲突时可选择全部跳过、全部覆盖或取消导入
- 导入时校验用量、价格、税率等字段，并显示新增/更新/跳过/失败明细
- 导入价格为 BOM 价格快照，不会自动读取物料管理最新价格
- 支持下载 BOM 导入模板（`BOM导入模板.csv`）
- 权限键：`bom.export`、`bom.import`

### 机型成本导入导出

- 机型成本支持导出 CSV（`机型成本_yyyyMMdd_HHmmss.csv`）
- 机型成本支持 CSV 导入，按机型编号 + BOM编号 + BOM版本 判断重复
- 导入时总成本自动等于材料成本；忽略人工成本、制造费用、其他费用、运费字段
- 支持下载机型成本导入模板
- 权限键：`model_cost.export`、`model_cost.import`

## Ver2.3

当前代码版本：冠誉ERP管理系统 Ver2.3。

### .NET 8 兼容

- 保留 Ver2.3 业务功能并迁移到标准 .NET 8 WinForms 项目
- 使用 `ERP.csproj` 和 `dotnet build` 编译
- JSON 处理改用 `System.Text.Json`

### 生产管理

- 新增左侧菜单分组「生产管理」，包含 BOM表、机型成本
- 菜单位于「仓库管理」与「财务管理」之间

### BOM表

- 支持 BOM 新增、编辑、删除、列表查看与搜索
- BOM 主表字段：BOM编号、机型编号、机型名称、产品名称、版本号、状态、BOM总材料成本、备注、创建时间、更新时间
- BOM 明细从物料管理选择物料，自动带出编号、名称、单位、原始单价、价格类型
- 明细支持新增行、删除行、修改用量与备注
- 金额 = 用量 × 不含税单价；BOM 总材料成本自动合计
- 含税价按系统税率换算：不含税单价 = 含税单价 ÷ (1 + 税率 / 100)
- 保存时锁定每条明细的价格快照（原始单价、价格类型、税率、不含税单价、金额、价格来源时间）
- 物料管理价格为当前最新价；已保存 BOM 使用保存时的价格快照，不会随物料涨价/降价自动变化
- 编辑 BOM 时可点击「重新取最新物料价格」手动更新全部明细价格
- 保存时锁定每条明细当时使用的税率，后续税率修改不影响已保存 BOM
- 数据保存在 `bom.json`

### 机型成本

- 支持机型成本新增、编辑、删除、列表查看与搜索
- 选择 BOM 后自动带出机型编号、机型名称、产品名称、BOM编号、BOM版本、材料成本
- 机型成本当前仅统计 BOM 材料成本（不含人工、制造、运费等其他费用）
- 总成本 = 材料成本
- 材料成本来自 BOM 总材料成本（价格快照），不会随物料管理最新价格自动变化
- 数据保存在 `model_costs.json`

### 税率修改

- 系统设置新增「税率修改」入口
- 默认税率 10%，保存到 `system_settings.json`
- 权限键：`settings.tax_rate`

### 物料管理增强

- 新增「价格类型：含税 / 不含税」字段，与原有含税价、不含税价并存
- 旧物料无价格类型时默认按「不含税」处理

### 权限扩展

- BOM表：`bom.view` / `bom.add` / `bom.edit` / `bom.delete`
- 机型成本：`model_cost.view` / `model_cost.add` / `model_cost.edit` / `model_cost.delete`
- 税率修改：`settings.tax_rate`
- 权限同时控制左侧菜单、页面按钮、后端 API

## Ver2.2

冠誉ERP管理系统 Ver2.2。

### 账号管理与权限管理

- 新增账号数据文件 `users.json`，保存在 `%ProgramData%\智造ERP供应商管理\` 目录
- 首次启动且 `users.json` 不存在时，自动创建默认主账号 `admin`（系统管理员，密码 `admin123`，SHA256 哈希存储）
- 若 `users.json` 已存在，程序不会覆盖，已修改密码在重启或升级后保持不变
- 系统管理员可新增、编辑、删除子账号，并为子账号分配模块级权限
- 子账号默认角色为「普通用户」，默认无业务权限，需管理员手动勾选
- 权限采用「模块 + 操作」结构，覆盖供应商、客户、物料、财务收支及系统设置
- 权限同时控制左侧菜单、页面按钮及后端 API 接口，无权限时统一提示「无权限操作」
- 主账号 `admin` 拥有全部权限，不可删除、不可禁用、不可修改权限，可修改自己的密码
- 系统设置新增可用功能：账号管理、修改密码
- 首页看板对所有已登录用户开放；各看板区域按 view 权限显示数据或「无权限查看」

## Ver2.1

当前代码版本：冠誉ERP管理系统 Ver2.1。

### 个性化设置

- 新增正式个性化设置页面，包含：显示大小、主题颜色、表格密度、默认打开页面、删除确认开关
- 显示大小与右上角下拉框双向同步，保存到 localStorage
- 主题颜色支持：默认蓝、商务灰、护眼绿、紫色
- 表格密度支持：紧凑、标准、宽松，应用于首页看板及各业务表格
- 默认打开页面在登录成功后自动跳转，不影响手动切换菜单
- 删除确认开关默认开启，可应用于供应商、客户、物料、财务删除操作

### 系统设置

- 系统设置改为结构化页面，包含 5 个入口：账号管理、修改密码、数据备份、数据恢复、系统参数
- 各入口点击后显示占位内容「功能开发中」，后续版本逐步实现

## Ver2.0

当前代码版本：冠誉ERP管理系统 Ver2.0。

### 菜单结构重组

- 首页单独作为左侧一级菜单项
- 左侧菜单重新分组为：资料管理、销售管理、仓库管理、财务管理、设置
- 已有功能入口保留：首页、供应商管理、客户管理、物料管理、财务收支
- 新增占位菜单：个性化设置、销售订单、销售出库、采购单、采购入库、领用出库、库存、应收款、应付款、系统设置
- 占位菜单点击后显示「该功能开发中」，不报错
- 未修改业务逻辑、API 接口和数据格式

## Ver1.9

当前代码版本：冠誉ERP管理系统 Ver1.9。

### 菜单调整

- 删除左侧导航栏中的「采购管理」和「库存管理」占位菜单入口
- 保留「物料管理」及现有业务模块：首页、供应商管理、客户管理、财务收支
- 未修改业务逻辑、API 接口和数据格式

## Ver1.8

当前代码版本：冠誉ERP管理系统 Ver1.8。

### 打包规则优化

- `build.ps1` 自动扫描 `02-安装包` 目录中已有安装包，识别最新版本号并生成下一个版本
- 版本递增规则：`Ver1.7 → Ver1.8 → Ver1.9 → Ver2.0 → Ver2.1`，以此类推
- 安装包命名固定为 `冠誉ERP管理系统Ver版本号.zip`
- 安装包输出目录固定为 `C:\Users\Administrator\Documents\冠誉ERP项目\02-安装包`
- 新版本 zip 生成成功后，自动解压到 `冠誉ERP管理系统Ver版本号` 文件夹
- 旧版本 zip 和旧版本解压文件夹自动移动到 Windows 回收站，不永久删除
- `02-安装包` 目录最终只保留最新版本 zip 和最新版本解压文件夹
- `build.ps1` 不会自动修改 `README.md` 和 `VERSION.md`，版本文档仍由开发时手动维护

## Ver1.7

当前代码版本：冠誉ERP管理系统 Ver1.7。

### 批量操作

- 供应商、客户、物料管理新增批量删除：行复选框、表头全选、二次确认、自动刷新表格和统计卡片
- 供应商、客户、物料管理新增批量添加：弹窗多行录入、继续添加行、删除临时行、统一校验保存
- 批量添加采用部分成功策略：正确数据保存，重复或错误数据跳过并显示原因；全部错误时不保存
- 新增后端接口：`/api/suppliers/batch`、`/api/suppliers/batch-delete` 及客户、物料对应接口
- 保留现有单条新增、编辑、删除和 Excel 导入功能

### 界面优化

- 全局显示大小切换：标准 / 大 / 超大，设置保存在浏览器 localStorage
- 放大字体、按钮、输入框、表格行高和主内容区域宽度
- 供应商、客户、物料宽表格增加顶部横向滚动条，与底部滚动条双向同步

### 兼容性

- 未修改财务模块和首页看板业务逻辑
- 保持现有 JSON 数据格式、备份方式和局域网访问方式不变
- 安装包名称更新为 `冠誉ERP管理系统Ver1.7.zip`

## Ver1.6

当前代码版本：冠誉ERP管理系统 Ver1.6。

### 一键编译与打包

- 新增 `GuanYuERP.csproj`，支持通过 MSBuild 编译 WinForms + HttpListener 项目
- 新增 `build.ps1`，一键编译并生成安装包
- 新增 `安装说明.txt`，随安装包分发给最终用户
- 安装包输出路径：`C:\Users\Administrator\Documents\冠誉ERP项目\02-安装包\冠誉ERP管理系统Ver1.6.zip`
- 安装包包含：`GuanYuERP.exe`、`安装说明.txt`、`VERSION.md`
- 更新 `.gitignore`，避免 exe、zip、编译中间目录和业务数据进入 Git 仓库

### 兼容性

- 未修改 `Program.cs` 及四个 HTML 页面中的业务逻辑
- 保留 Ver1.5 的首页看板、供应商、客户、物料和财务功能
- 保持现有数据目录、数据格式、备份方式和局域网访问方式不变

## Ver1.5

冠誉ERP管理系统 Ver1.5。

### 首页综合看板

- 首页新增客户应收明细表，读取现有客户档案中的当前应收款并计算合计
- 首页新增供应商应付明细表，读取现有供应商档案中的当前应付款并计算合计
- 首页新增库存物料表，读取现有物料档案中的物料、数量/单位、供应商和状态
- 首页新增财务收支表，读取现有财务流水并汇总收款、付款金额
- 首页数据只读取现有接口与 JSON 数据，不新增或修改真实业务数据
- 返回首页时自动刷新四张数据表

### 兼容性

- 保留 Ver1.4 的供应商、客户、物料和财务页面及业务逻辑
- 保持现有数据目录、数据格式、备份方式和局域网访问方式不变

## Ver1.4

该版本为冠誉ERP管理系统 Ver1.4。

### 基础运行能力

- Windows 单实例托盘程序
- 内置 `HttpListener` Web 服务，固定使用端口 `8787`
- 自动打开本机浏览器
- 支持局域网内多台电脑访问同一套数据
- HTML 页面以内嵌资源方式随程序发布

### 用户与审计

- 用户登录、退出和内存会话管理
- 内置管理员、采购、财务和管理层账号定义
- 登录及数据操作日志
- 未登录请求统一返回认证错误

### 供应商管理

- 供应商档案新增、查询、编辑和删除
- 自动生成 `GY` 前缀编号
- 联系人、供应商品、地址、银行资料、应付款和状态维护
- `.xlsx` 导入及 CSV 导出
- 供应商数量、联系人完整度、开票资料和商品种类统计

### 客户管理

- 客户档案新增、查询、编辑和删除
- 自动生成 `KH` 前缀编号
- 联系人、地址、银行资料、应收款和状态维护
- `.xlsx` 导入及 CSV 导出
- 客户数量、联系人完整度、开票资料和启用状态统计

### 物料管理

- 物料档案新增、查询、编辑和删除
- 自动生成 `WL` 前缀编号
- 关联已建档供应商
- 名称规格、数量单位、含税价、不含税价、备注和状态维护
- `.xlsx` 导入及 CSV 导出
- 物料数量、关联供应商、含税价格和启用状态统计

### 财务管理

- 收款和付款流水新增、查询、编辑和删除
- 公户、公司私户和个人私户分类
- 三类账户期初余额设置及实时余额计算
- 按年度、月份、账户和关键词筛选
- 月度经营情况及收付款用途统计
- 财务明细 CSV 导出

### 数据与备份

- 供应商、客户、物料、财务及期初余额分别使用 JSON 文件保存
- 使用临时文件替换方式保存主要业务数据
- 数据修改前自动生成备份
- 支持界面和托盘菜单手动备份
- 自动清理旧备份，最多保留 50 个 JSON 备份文件
- 启动时为旧数据补齐业务编号

### 尚未完成

以下模块在 Ver1.4 中仅有菜单入口，尚未实现业务页面或后端接口：

- 采购管理
- 库存管理
- 应付账款
- 系统设置
- 首页综合看板

### 当前版本说明

- 数据默认目录：`C:\ProgramData\智造ERP供应商管理`
- 当前未提供 `.sln` 或 `.csproj`，通过 .NET Framework C# 编译器构建
- 当前角色信息用于展示，尚未实施细粒度接口权限控制
- 当前版本面向可信 Windows 局域网环境运行
